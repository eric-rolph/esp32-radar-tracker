"""Shared toolkit for the BeamNG maps pack (real-terrain levels from public GIS data).

Mirrors ``examples/giant_props/proplib`` in beamng-mcp: a deterministic generator owns
every number, ``authoring/`` holds the evidence, ``mod/`` and ``dist/`` are build output.
"""
